// API ENDPOINTS: Centralized configuration for all API URLs
// Makes it easy to update endpoints in one place
const apis = () => ({
  getAssignment: "/api/assignments",
  createAssignment: "/api/assignments",
});

export default apis;